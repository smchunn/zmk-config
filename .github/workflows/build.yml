on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to match the zmk source revision in config/west.yml (v0.3.0). The @main
    # reusable workflow added a Zephyr-4.1 "ZMK compat" post-build check that rejects
    # the pre-4.1 boards we build. Keep this in lockstep with west.yml.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
